# Changelog

## Unreleased

- Synchronize OpenAPI companion and docs with native durable identity, auth abuse controls, and saved/liked/playlist resource return shapes.

- Add native password, Google identity, account lifecycle, and per-device session RPCs to `canopy.v1.AuthService`.
- Establish the audited `canopy.v1` bounded-service contract.
